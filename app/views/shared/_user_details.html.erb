<div class="user-top">
    <%= image_tag @user.photo , size: '200x100', alt: @user.name + 'photo' %>
    <div class="top-detail">
      <h1 class="name"><%= @user.name %></h1>
      <p class="posts-number">Number of posts: <%= @user.posts_count %></p>
  </div>
</div>
